Frontend:
Open the "index.html" file in your web browser by double-clicking on it. The frontend application will load in the browser.

Node.js Backend:
Start the Node.js server by command: node Node.js
The server will start, and you will see a message indicating that it's running on "http://localhost:3000"